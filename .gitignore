node_modules/
dist/
*.tsbuildinfo
.env
.env.local
coverage/
.vitest/

# Datos de origen descargados (grandes, regenerables con pnpm data:fetch-airports).
# El snapshot normalizado en data/*.json SÍ se versiona: ver ADR-011.
data/source/

# Salida de la vista previa: se construye con `pnpm ui:preview`.
# La instantánea de datos que la alimenta (data/ui-snapshot.json) SÍ se
# versiona: sin ella el despliegue no puede construirse, porque generarla
# exige una base de datos con un mundo simulado.
dist/

# Salida de compilación de los scripts de construcción.
.tsbuild/
